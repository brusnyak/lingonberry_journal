# ── Python ──
.venv/
__pycache__/
*.pyc
.pytest_cache/

# ── OS ──
.DS_Store

# ── Secrets / keys ──
.env
*creds*.txt
*creds*.key
*auth.txt
*account-id*.txt
OANDA-*.zip
!.env.example
*.key
*.key.pub

# ── Data files (large, generated) ──
*.csv
*.parquet
*.png
*.db
*.json
!opencode.json
!opencode.jsonc
data/
data/.ctrader_sync_state.txt
data/.bot.pid
data/.webapp.pid
data/.ctrader_sync.pid
data/.sltp_poller.pid

# ── Hermes (external agent files — not project code) ──
hermes/

# ── Generated reports (rebuild on demand) ──
backtesting/results/*.html

# ── Scratch / random text files ──
qwen37.txt

# ── Docs (full burn — rebuild when needed) ──
docs/

# ── Charts ──
charts/

# ── Generated analysis data ──
pine-review/data/
pine-review/logs/
pine-review/__pycache__/

# ── Webapp (node deps are build artifacts) ──
webapp/node_modules/

# ── IDE ──
.vscode/
